 //let arr1 = [12 , 23.8 , 'Hello' , True];  
let color = ['red' , ['yellow' , 'orange' ], 'Blue'];
/* console.log(color[1]);
for(let i = 0; i < color.length; i++){
    console.log(i, color[i]);
}

for_in

for(let i in color){
    console.log(i, color[i]);
}

for-of


for(let v of color){
    console.log(v);
}

let newArr = color.map((data, i) => `color at ${i} is ${data}`)
    return`color is $(data)`
 */
/* let newArr = color.filter(data => data != 'red')
    console.log(color);
    console.log(newArr); */

    console.log(color);
/*     color.push('Voilet' ,'Pink');
    console.log(color.pop());
    console.log(color); */

    /* color.unshift('Pink', 'Voilet');

    console.log(color.shift());
    delete color[2]; */
   
    // color.splice(1,2, 'Pink', 'Voilet', 'White', 'Gold'); //kaha dekhi kate ota
    console.log(color[1][1]);
    console.log(color[2][0]);


